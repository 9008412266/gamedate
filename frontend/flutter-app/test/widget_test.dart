import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Playraze app smoke test', (WidgetTester tester) async {
    // Basic smoke test — app launch চেক করুন
    expect(true, isTrue);
  });
}
